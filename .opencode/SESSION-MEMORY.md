# Session Memory — CubeApp

## Project
C# `net9.0-windows` WinExe (self-contained), Veldrid 4.9.0 (D3D11), SPIR-V, ImGui, StbImageSharp, Nullable enabled.
Build: `dotnet build CubeApp.csproj -c Debug --nologo -v minimal` → 0 errors, ~38 pre-existing CS8618 warnings.
Run target: `bin\Debug\net9.0-windows\win-x64\CubeApp.exe` — the `bin\Debug\net9.0-windows\` parent-folder exe is a stale 7/3 apphost bootstrap that CRASHES on launch. Always run the win-x64 one. Verify the DLL timestamp freshened after each build. Repo: `https://github.com/MegaYote/CubeApp.git` (branch `main`).

## World/Vertex Format
- Vertex: 13 floats (`aPosition` F3, `aLocalUV` F2, `aTileRect` F4, `aColor` F4); `VertexStrideBytes = 52`.
- Chunks 16x256x16, `OriginY = -64`, sea level Y=0; sky light 0..15, Manhattan falloff, spreads horizontal/down only (no +Y in `SkyDirs`).
- D3D11: indirect-args buffers must NOT be `BufferUsage.Dynamic` (else `E_INVALIDARG`). `IndirectCommandStride = 20`, initial VB cap 4MB / IB cap 2MB, chunk-local UInt16 indices, `FirstIndex`/`VertexOffset`.

## Water (Infdev 20100630 port) — COMPLETED
- **All four phases build clean**: Phase 1 meta in `Chunk.cs`, Phase 2 `ChunkManager` meta + loaded-only writes, Phase 3 `FluidSimulation` + `BlockTickScheduler` (20 TPS, `MaxUpdatesPerTick=2048`, flushes `_meshScheduler.Update()`), Phase 4 water meshing in `Mesher.cs`.
- `FluidSimulation.BlockFlowing` is a faithful port of `BlockFlowing.java`; water tick 5; `OnBlockChanged` wakes self + neighbors; `onBlockAdded` self-wake fix.
- Water: `blocks.json` `"texture":"12,14"`, `"solid":false`, `"opaque":false`, `"transparent":true`, `"alpha":0.65`. `sideTile = baseTile + TileSize` on X.
- **Mesher**: `EmitWaterFaces` (water-only faces), `EmitWaterCellFaces`, `EmitWaterSide` (4-point signature), `GetFluidHeight`, `GetBlockAt`/`GetMetaAt`/`FindChunk` (missing chunk → air/0 via `chunkLookup`). `getPercentAir` clamps meta ≥ 8 → 0 exactly as reference.
- **Chunk-border geometry**: `MeshWorker` passes target + 4 cardinals + 4 DIAGONAL neighbors to `GenerateMesh` (corner heights sample the 2x2 block neighbourhood around each corner, which crosses into the diagonal chunk at a 4-chunk junction). `MarkDirty` in `ChunkManager` flags cardinal AND diagonal border neighbors.
- **Side-face winding fixed** (was culled from view side): all four `EmitWaterSide` quads use bottom-pair-first + sloped top edge, matching the greedy pass ordering.
- **Transparent two-pass renderer fix (8/4, verified by user "it looks awesome now")**: water walls at chunk borders rendered ghosty/see-through because the single blended pass with depth-write ON let a water chunk drawn first depth-block the terrain behind it. Fix in `VeldridRenderer.cs`: `BuildMesh` splits faces into opaque (alpha ≥ 1) + transparent (alpha < 1) arrays; each chunk uploads TWO `ChunkRange`s into the same mega buffers (`_chunkRanges` + `_transparentRanges`); `RebuildDrawCommands` builds two command lists; draw loop calls `DrawWorldPass` twice — opaque with `_pipeline` (depth-write on), then transparent with `_transparentPipeline` (SingleAlphaBlend, `DepthStencilStateDescription(true, false, LessEqual)` = depth test on, write off). `PendingUpload` carries both pairs; `FreeChunkRange` frees both.
- **Known small artifact (not the report, not yet fixed)**: side-wall UV in `TryGetCubuildFaceAxes` maps V from Y with `vAxis=(0,-1,0)` — shows the top strip of the side tile; MC anchors the tile bottom at the waterline instead. Optional follow-up: MC-style side-wall UV anchoring.
- **Water rendering fixes (8/5, verified by user "yay <3 pretty blue water")**:
  - **THE BIG ONE — light Y mismatch**: the water pass called `ChunkLighting.GetLight` with WORLD Y (-64..191) but the light array is indexed by LOCAL Y (0..255, OriginY=-64). Sea-level water (world Y=0) read array index 0 = the BOTTOM bedrock block → light 0 → brightness floor 0.05 → water RGB crushed to near-black. Fixed: all three water light samples in `Mesher.cs` convert `wy - ChunkManager.WorldOriginY` (top face `ly`, `ly+1`; bottom `ly-1`; side `wy - WorldOriginY`). The greedy pass always used local Y, which is why terrain was lit fine.
  - **Side-wall UV anchoring (AnchorVBottom)**: added `MeshFace.AnchorVBottom` flag, set true in `EmitWaterSide`. `BuildMesh` shifts dv by `(1 - (maxV-minV))` so the tile bottom anchors to the block bottom and the surface cuts across — matches `RenderBlocks.renderBlockFluids` `((var51 + (1.0F - var31) * 16.0F) / 256.0F)`. Opaque faces (h=1) get offset 0, unaffected.
  - **Shader opacity fix**: fragment shader now `outColor = vec4(tex.rgb * vColor.rgb, vColor.a)` — block alpha governs opacity, not the atlas art's baked alpha (water tile ships ~138/255 ≈ 54% in terrain.png, halving effective opacity to ~46%).
  - **Water block alpha**: `blocks.json` water `alpha` 0.65 → 0.85.
  - **E-menu inventory (8/5, user-verified "perfect")**: press E toggles `inventoryOpen` in Program (releases mouse look when opening, re-locks when closing). `BuildHud()` passes `InventoryOpen` + per-slot hotbar contents (`_hotbarBlocks`, initialized from `BlockRegistry.Hotbar`) via `HudState`. Renderer: `SetUiInputSnapshot(snapshot)` feeds the real `InputSnapshot` to `_imguiRenderer.Update` (was `NullInputSnapshot.Instance` — ImGui was fully inert before), `DrawInventoryWindow` renders an ImGui grid of every block (icon + tooltip), clicks enqueue into `_inventorySelections` (ConcurrentQueue). Program polls `TryTakeInventorySelection` after `Render()` and drops the pick into `_hotbarBlocks[selectedSlot]` + closes + re-locks. `SetSelectedSlot` reads `_hotbarBlocks[slot]`. ImGui 1.89 gotcha: `ImageButton` takes a STRING id FIRST: `ImageButton($"##icon{id}", texId, size, uv0, uv1, bg, tint)` — no frame_padding param. New methods added to `IRenderer` too.
- **Cross plants + 4-pass renderer (8/5, user-verified "perfect")**: added 8 cross-shape blocks (sapling, red/yellow/blue flower, red/white/poison mushroom, spikes) via `"shape": "cross"` in blocks.json. `BlockRegistry` gained fast `_cross`/`_cutout`/`_glass` bool arrays + `IsCross/IsCutout/IsGlass`. The MESHER routes faces by alpha sign: opaque = alpha>=1, cutout (leaves/cross) = -alpha, glass = -alpha-100, transparent (water) = 0..1. `Mesher.EmitCrossFaces` builds 2 diagonal billboard quads per plant (vertex order top-first so the edge-based UV doesn't flip the sprite). Renderer now has FOUR passes/ranges/pipelines (opaque, cutout, glass, transparent), each with its own command list + indirect scratch:
  - `_pipeline` — opaque, depth write on, cull back.
  - `_cutoutPipeline` — separate discard shader (`if (tex.a < 0.5) discard;`), depth write ON, cull NONE (matches Cubuild worldMaterialCutout). Fixes far-quad-paints-over-near via depth.
  - `_glassPipeline` — same discard shader, depth write OFF, cull BACK (matches Cubuild worldMaterialGlass). Far glass shows through near panes.
  - `_transparentPipeline` — water, blend, depth write off, cull NONE.
  - Glass + transparent passes are `SortPassBackToFront`'d (chunk center dist from camera, far first) each frame — Cubuild/THREE.js behavior so near glass paints over far.
  - The user rejected the transparent depth-prepas approach in favor of this exact Cubuild architecture. Draw order: opaque -> cutout -> glass -> transparent.
  - BuildMesh is a 4-way split; PendingUpload carries 8 arrays; WriteChunkData uploads 4 ranges.
- **Slabs + stairs (8/5, user-verified "perfect")**: 18 new blocks (stone/planks/cobblestone/sand/dirt/bricks × bottom slab, top slab, stairs). Data: `"shape": "slab" | "slab_top" | "stairs"`; top variants get `"inventory": false` (hidden from E-menu; BlockRegistry gained `Inventory` flag + `IsInInventory`, DrawInventoryWindow filters). Only ONE slab per material shows in the menu; placement auto-picks top vs bottom by click position (`placeTop = normal.Y < 0 || (normal.Y == 0 && hitPoint.Y - place.y > 0.5)`). Merging: `TryMergeSlab` (hit-cell merge: aiming at a bottom slab's top or a top slab's bottom fuses to full block) + `TryFillSlabCell` (filling an OCCUPIED place cell's opposite half fuses too; otherwise refuse to place). `Mesher.EmitSpecialFaces`/`EmitBox` render slabs/stairs as partial boxes with per-face occlusion, sampled from the NEIGHBOR cell (local Y!); plane-based occlusion (a face only hides against a full cube if it's ON the cell boundary — otherwise a block above a slab would wrongly hide its top); FaceVertices +Z/-Z entries are wound opposite their normals so EmitBox must flip (Dot(Cross(p1-p0,p2-p0), normal) < 0 → swap p1/p3) or those faces get back-face culled. `RendersToward` shows cube faces toward partial shapes. Collision: `GetBlockCollisionBoxes(id, meta)` returns slab half-box / stair two-boxes (matching mesher) / small centered box for cross plants; `IsPlayerColliding` + `WouldBlockIntersectPlayer` use them.
- **Step-up (8/5, user-verified "perfect")**: `TryStepUp` in Program — on any horizontal collision, lift 0.5, retry the move, settle down (MC auto step-up). CRITICAL: no `!hitY` gate (a grounded player collides downward every frame, which would disable stepping entirely).
- **Shape-aware ray pick (8/5, user-verified "perfect")**: `TryPickBlock` now tests the ray against the block's actual collision boxes (`RayBoxHit`, t-limited to the current DDA cell). The ray passes through the empty half of partial blocks (continues to later cells). Returns exact hit distance, face normal, and the world-space `Face` rect (`ComputeFaceRect`) so the highlight hugs real shapes (slab top at y+0.5, stair riser). `place = hitCell + normal`. `ComputeHighlightWorldQuad` builds from the Face rect. Cross plants get a small box (aim past them). Pitfall: place can now be an OCCUPIED cell → slab placement must handle the merge-or-refuse (see above), and general placement refuses to overwrite.
- **Random world seed + Infdev-style terrain (8/5, user-verified "perfect")**: `Program` rolls `worldSeed = Random.Shared.Next(0, int.MaxValue)` each launch and passes it to `InfdevChunkProvider` (seed feeds `HashToUnit`). Shown as `Seed: N` in the F3 debug overlay (`HudState.WorldSeed`). TERRAIN RETUNE: the old heightfield was `64 + base*72 + detail*24` — baseline 64 blocks ABOVE sea level (world Y 0), so no oceans ever formed. New: `surfaceWorldY = round(continent*16 + hills*8 + 1)` where continent = `Fbm2D(0.0025, 3 octaves)` (broad ~400-block landmasses/ocean basins) and hills = `Fbm2D(0.012, 3 octaves)`. Surface sits at sea level → real oceans with sandy shores, gentle rolling plains. World Y range -64..191 intentionally UNCHANGED (user wants deep digging + tall building). `FindSafeSpawnPosition` widened to radius 14 and picks the HIGHEST solid column (spawns on best land, not ocean floor).
- **3D density terrain + biomes (8/5, user-verified "perfect")**: `InfdevChunkProvider.GenerateChunk` rewritten from a 2D heightfield to a 3D density field: `density = (targetSurface - worldY) + main3D*5 + overhang*2.5` (main3D = Fbm3D(0.012, 0.024, 0.012, 2), overhang = Fbm3D(0.033, 0.045, 0.033, 1)). Gives overhangs, ledges, natural cave mouths. REGION/BIOME masks: `region = Fbm2D(0.0032, 3)` → `plainsW/mountainW/hillsW` smooth masks; targetSurface = continent*20 (Fbm2D 0.0024) + plainsW*roll*6 + hillsW*roll*16 + mountainW*(roll*26+rugged*16) + 1. CRITICAL BUG WATCH: the column fill loop must scan TOP-DOWN so `firstSolid` hits the surface — bottom-up put grass at the bedrock (world looked like all stone!). Surface = first solid from top (grass/sand), 3 blocks of dirt/sand, then stone. Water fills air below sea level only while `firstSolid` (above the sea floor); underwater caves stay air. `BiomeNameAt` returns Ocean/Mountains/Hills/Plains for the F3 overlay.
- **Fly mode (8/5, user-verified "perfect")**: F2 toggles `flyMode` (InputProcessor Key.F2 → ToggleFlyPressed → PlayerAction/FrameInputState). TickInputState gained MoveUp (Space held) + MoveDown (ShiftLeft held). Fly: full 3D pitch-aware movement at FlySpeed=10.8, no gravity, collisions on. F3 shows Fly: ON/OFF.
- **Block-break particles (8/5, user-verified "perfect" after fixes)**: `VeldridRenderer` CPU particle pool (512), `SpawnBlockBreakParticles(x,y,z,blockId,count)` called in `DeleteHighlightedBlock` before removal. Particles = camera-facing textured quads drawn with the world `_pipeline` (atlas + depth test) after the transparent pass. GOTCHAS: (1) don't early-return on `_particleVertexBuffer == null` BEFORE `EnsureParticleBuffers` — first-use never created the buffer; (2) UVs must never hit exactly 1.0 because the world shader samples via `fract(vLocalUV)` and `fract(1.0)==0.0` collapses the quad onto one texel — use 0.999; (3) each particle samples a random ~4x4-texel crop of the tile (`pieceW = tileW/4` + random offset), not the whole tile. Gravity 18, velocities VX/VZ ±2.2, VY 0.8-3.5. Camera billboard basis `_cameraRight/_cameraUp` stored in `UpdateCamera`.
- **Safe spawn (8/5)**: `FindSafeSpawnPosition` now scans outward RING BY RING (generating chunks on demand via `manager.GetOrCreateChunk`) and takes the FIRST ring with DRY land (surface world Y >= 0). Within a ring, the highest dry spot wins. Never spawns in water (the old version picked the highest solid in a fixed radius — could be the ocean floor). `FindSurfaceWorldY` scans world Y 191..-64 for the topmost solid. Infdev reference spawns on SAND (`findSpawn` = top block is sand); we generalize to "any dry land". Pitfall: the old scan passed local-Y values as world Y to TryGetLoadedBlock (quirk); the new helper uses proper world Y.
- **Water is a fluid, not a pickable block (8/5, user-verified "perfect")**: `TryPickBlock` skips water cells entirely (ray passes through to the block behind/underneath — like Infdev). Water can't be targeted/broken. Placement can REPLACE water: `IsReplaceableFluid(id)` (currently just water); the slab-branch and general-branch occupancy checks both allow placing into water cells (displacing it). Player already passes through water (never solid). NOTE: `IsReplaceableFluid` calls `BlockRegistry.GetId("water")` — fine since water always exists.
- **Title screen / menus (8/5, user-verified)**: `MenuState.cs` (new) holds `GameScreen` enum (Title/CreateWorld/Playing/Paused) + world name/seed + button flags (`CreateWorldClicked`, `ResumeClicked`, `QuitToTitleClicked`, `QuitClicked`) shared between Program (owns flow) and the renderer (draws ImGui menus). Program boots to Title with NO world (`StartNewWorld(seed)` creates provider/manager/workers + spawns + enters Playing; world objects are non-readonly now). `ProcessMenuActions` polls the flags each frame. ESC toggles Playing<->Paused. `StepSimulation` guards `screen == Playing`. `_ignoreInteractFrames = 2` set on world-start/resume so the menu click that resumes doesn't also break a block; auto-enroll mouse look is gated to Playing. CRITICAL: before a world exists the render path NPE'd on `entityManager.MobRenderData` / null manager — `BuildHud` returns a safe menu-only state, entity rendering is guarded, `meshScheduler` null-guarded. Renderer menus: dirt background via `ImGui.GetBackgroundDrawList()` (NOT foreground — foreground paints OVER the windows!), logo (embedded `cubuild.png`, loaded like the duck texture, bound to ImGui, drawn top-center like Infdev), buttons column lower on screen (MC style, ~18px gaps). `windowFlags` = NoTitleBar|NoResize|NoMove|NoCollapse|NoScrollbar|NoScrollWithMouse (the logo overflowed its window + padding so the wheel scrolled it). MenuState buffers: `_worldNameBuffer/_seedBuffer` byte[] + `WriteBuffer/ReadBuffer` helpers; `ImGui.InputText` takes the buffer BY VALUE (not ref) and `ImGui.Begin` wants `ImGuiWindowFlags` (not uint). `ResetWorld()` added to the renderer (clears all chunk ranges/commands) for starting a new world over the old one.
- **World save/load (8/5, user-verified)**: `WorldSave.cs` — binary format `"CUBW"` + version, world name, seed, player pos/look, hotbar(10), then modified chunks (block+meta byte arrays, 65536 each) and mobs, all Deflate-compressed to `saves/<name>.cubuild` next to the exe (`AppDomain.CurrentDomain.BaseDirectory`). ChunkManager tracks `_modifiedChunks` (HashSet, hooked in `MarkDirty`) + `ApplySavedChunk(cx,cz,blocks,meta)` stamps over the regenerated chunk. SAVE TRIGGERS (user's call): ESC pause-open, "Save & Quit to Title", window close. LOAD: `LoadWorld(save)` = `StartNewWorld(seed, name)` (regen) then apply chunks + restore player pos/yaw/pitch + hotbar + `entityManager.LoadMobs(save.Mobs)`. MOBS: `MobEntity.RestoreState(pos,yaw,health)` + `Duck.RestoreState`; `EntityManager.SaveMobs()/LoadMobs()` map types duck/coyote/steve/GenericMobEntity(by `MobId`). `GenericMobEntity.MobId => _definition.Id`. TITLE: "Saved Worlds" list (first 6) below the buttons — click to set `SelectedWorldIndex` + `LoadWorldClicked`; `RefreshSavedWorlds()` scans the saves folder (called at boot + on ReturnToTitle). PAUSE MENU: translucent gray wash (`GetBackgroundDrawList().AddRectFilled` rgba(0,0,0,0.45)) over the frozen world instead of the dirt background (dirt stays for Title/CreateWorld). Save can hitch briefly (clones all modified chunks) — acceptable.
- **Probe harness**: `C:\Users\jimza\AppData\Local\Temp\opencode\waterprobe\` — self-contained console app referencing CubeApp project; generates chunks, builds lighting, prints water light values + mesh face alphas. IMPORTANT: the FIRST chunk in the list passed to `Mesher.GenerateMesh` is the mesher's target — put chunk (0,0) first when probing it.
- **Block catalogue port (8/5)**: ported 25 more full-cube blocks from the old `Cubuild.html` project (same atlas convention `tile(row,col)` == blocks.json `"col,row"`, verified against terrain.png). `blocks.json` now has 37 blocks: added bricks, log (side/top differ), leaves (alpha 0.85), glass (alpha 0.4), bomb, full_grass, sponge, wool, cage, darkstone, bluebrick, greenbrick, yellowbrick, sap, rottingwood, coalore, ironore, goldore, diamondore, bluestoneore, copperore, iron, gold, diamond, bookshelf. Grass now uses per-face tiles (top `0,0` / bottom `2,0` / side `3,0` = classic seam). Greedy pass uses `BlockRegistry.FaceTexture(normal)` so top/bottom/side overrides work.
- **Shader decision (8/5)**: block alpha (vColor.a) governs opacity, NOT atlas art's baked alpha. Rationale: water tile ships at ~54% baked alpha in terrain.png, which halved effective opacity; ignoring tex alpha keeps water at its configured 0.85. Tradeoff: glass/leaves render as uniform translucency (their per-pixel alpha is ignored) — classic per-pixel cutout (leaves holes) is a future alpha-test feature.
- **terrain.png is hard to modify on disk**: something holds an exclusive write lock (survived killing the VS Code C# Dev Kit language server + Restart Manager shows nothing). Don't plan around editing terrain.png in place; use block alpha in blocks.json instead.
- **NOT YET PORTED (needs engine features)**: slabs (`shape: slab_bottom/top`), cross-shape plants (sapling, red/yellow/blue flower, red/white/poison mushroom, spikes), torch (+ wall variants), fire, and lava (needs the fluid pass — lava would render as a cube via the greedy pass since only water is specially skipped).
- **Block icon atlas (8/5, user-verified "perfect")**: `VeldridRenderer.BuildIconAtlas()` renders a classic MC-style isometric cube icon per block into one RGBA texture (48px cells, 12/row) and exposes it to ImGui via `_imguiRenderer.GetOrCreateImGuiBinding(_gd.ResourceFactory, view)` (note: the binding API takes `(ResourceFactory, TextureView)`, not `(GraphicsDevice, Texture)`). Hotbar in `BuildHudUi` now `AddImage`s each block's cube instead of the flat map-color square. PROPORTIONS ARE THE EXACT Cubuild `drawProjectedBlockIcon` coords scaled to 48px cells: top=(24,4.5)(37.5,11.25)(24,18)(10.5,11.25), left=(10.5,11.25)(24,18)(24,37.5)(10.5,30.75), right=(24,18)(37.5,11.25)(37.5,30.75)(24,37.5). Affine inverses: top u=(su+2sv)/27,v=(2sv-su)/27; left u=(px-24+13.5)/13.5,v=(py-11.25-u*6.75)/19.5; right u=(px-24)/13.5,v=(py-18+u*6.75)/19.5. Face tiles via `FaceTexture` (top=+Y, left=-Z, right=+X). CPU atlas pixels captured in `_atlasRgba` from the decoded image. C# note: `float v` can't be declared at the loop-body level after a `float v` in a nested if-scope (CS0136) — use a distinct name (tv).

## Authoritative Shading Reference (user-provided): `IF-20100630-main` (Infdev 20100630, ~Alpha 1.1.2_01 era)
- `World.java` 1577-1585: `v = 1 - light/15`; `table[light] = (1-v)/(v*3+1) * 0.95 + 0.05` (gamma curve, 0.05 ambient floor).
- `RenderBlocks.java`: per-face multipliers bottom 0.5 / top 1.0 / N+S 0.8 / E+W 0.6; faces sample the ADJACENT EMPTY CELL they face into (e.g. bottom face samples `blockAccess, x, y-1, z`).
- **USER CONFIRMED (2026-07-31):** shading goal reached, Alpha 1.1.2_01 look accepted as-is. No further tuning requested.

## Regression harness
`C:\Users\jimza\AppData\Local\Temp\opencode\meshtest\` — console app referencing built `CubeApp.dll`; validates Brightness table + per-face shades. Run: `dotnet run` there.

## Build/run recipe
1. Kill any running `CubeApp` process.
2. `dotnet build CubeApp.csproj -c Debug --nologo -v minimal` in `C:\Users\jimza\CubeApp`.
3. Verify `bin\Debug\net9.0-windows\win-x64\CubeApp.dll` LastWriteTime freshened.
4. `Start-Process` the win-x64 exe.

## Next Moves
- Optional: MC-style side-wall UV anchoring (tile bottom fixed, top slides) for water walls — DONE 8/5 (AnchorVBottom flag).
- Optional: lava pass by copying the water simulation logic.
